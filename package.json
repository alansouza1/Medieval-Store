{
  "name": "medieval-store-root",
  "version": "1.0.0",
  "description": "Root orchestration for Medieval Store project",
  "scripts": {
    "backend:start": "docker exec -it trybesmith npm run start",
    "backend:dev": "docker exec -it trybesmith npm run dev",
    "backend:test": "docker exec -it trybesmith npm run test",
    "backend:lint": "docker exec -it trybesmith npm run lint",
    "backend:restore": "docker exec -it trybesmith npm run restore",
    "frontend:start": "docker exec -it trybesmith_frontend npm run start",
    "frontend:dev": "docker exec -it trybesmith_frontend npm run start",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  }
}
